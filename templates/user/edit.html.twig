{% extends 'base.html.twig' %}

{% block title %}Modifier l'utilisateur
{% endblock %}

{% block body %}
	<div class="container">
		<h1>Modifier l'utilisateur</h1>

		<div>
			<a class="btn btn-outline-secondary mb-2" href="{{ path('app_home') }}">
				<i class="bi bi-arrow-return-left me-2 mt-2"></i>Retour à la liste</a>
		</div>
		<div class="container col-lg-6 mt-lg-4 mx-auto pt-3 pb-3 text-center">

			<div class="mb-2">
				{{ include('user/_form.html.twig', {'button_label': 'Enregistrer'}) }}
			</div>
			{{ include('user/_delete_form.html.twig') }}
		</div>
	</div>

{% endblock %}
